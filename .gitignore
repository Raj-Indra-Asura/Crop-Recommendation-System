# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# --- Virtual environments ---
.venv/
venv/
env/
# Week 12's validation creates this one in place, to simulate a fresh install.
fresh-venv/

# --- Jupyter ---
.ipynb_checkpoints/
# Output of `jupyter nbconvert --to notebook --execute` without `--inplace`.
*.nbconvert.ipynb

# --- Trained model artifacts ---
# Model binaries are rebuilt on demand (see Week 9-10), never committed.
models/*.joblib
models/*.pkl

# IMPORTANT: data/ is deliberately NOT ignored.
# data/raw/Crop_recommendation.csv is small (~150 KB) and must stay
# version-controlled so every student runs against the identical dataset.

# --- OS noise ---
.DS_Store
