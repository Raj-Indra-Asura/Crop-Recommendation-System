# Crop Recommendation System - dependencies
# Target interpreter: Python 3.11
#
# This file is extended incrementally, one week at a time. Never rewrite it
# wholesale - each week appends the packages it introduces, and every package
# is explained in that week's docs/curriculum/weekXX/learning_notes.md.

# --- Week 1: load and validate the raw dataset, and set up the toolchain ---
# Data handling
numpy==2.2.1
pandas==2.2.3

# Visualisation (introduced now so the environment is built once; first used in Week 2)
matplotlib==3.10.0
seaborn==0.13.2

# Modelling (introduced now for the same reason; first used in Week 3)
scikit-learn==1.6.1

# Notebooks and guard rails
jupyter==1.1.1
pytest==8.3.4
ruff==0.8.4

# --- Week 7: gradient boosting (optional) ---
# XGBoost is the only optional package in this file. `get_gradient_boosting()`
# uses it when it is importable and falls back to scikit-learn's
# GradientBoostingClassifier when it is not, so the week is never blocked on
# this install - see docs/curriculum/week07/learning_notes.md.
xgboost==2.1.3

# --- Week 8: explaining individual predictions (optional) ---
# SHAP is the second and last optional package here. `explain_prediction()` uses
# it when it is importable and falls back to per-sample permutation plus the raw
# `predict_proba` breakdown when it is not, so the week is never blocked on this
# install - see docs/curriculum/week08/learning_notes.md. The pin is the last
# release that installs against the numpy/pandas/scikit-learn versions above.
shap==0.46.0

# --- Week 9: saving the trained pipeline to disk ---
# joblib arrives anyway as a scikit-learn dependency; it is pinned explicitly
# here because Week 9 calls it directly (`joblib.dump`/`joblib.load`), and an
# artifact written by one version must be reloaded by a version that can read
# it - see docs/curriculum/week09/learning_notes.md.
joblib==1.5.3

# --- Week 10: serving the pipeline over HTTP, and a demo UI ---
# FastAPI turns type hints into request validation and interactive docs; it is
# the ASGI framework, not the server. Pydantic does the validating and is
# already a FastAPI dependency - pinned explicitly because `api/schemas.py`
# imports it directly. Uvicorn is the ASGI server that actually listens on a
# port. Streamlit builds the demo UI (a demo, not a production frontend), and
# httpx is what `fastapi.testclient.TestClient` drives the app with in
# `tests/test_api.py` - see docs/curriculum/week10/learning_notes.md.
fastapi==0.115.6
pydantic==2.10.4
uvicorn==0.34.0
streamlit==1.41.1
httpx==0.28.1
