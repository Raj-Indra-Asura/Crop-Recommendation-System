# Crop Recommendation System - dependencies
# Target interpreter: Python 3.11
#
# This file is extended incrementally, one week at a time. Never rewrite it
# wholesale - each week appends the packages it introduces, and every package
# is explained in that week's docs/curriculum/weekXX/learning_notes.md.

# --- Week 1: load and validate the raw dataset, and set up the toolchain ---
# Data handling
numpy==2.2.1
pandas==2.2.3

# Visualisation (introduced now so the environment is built once; first used in Week 2)
matplotlib==3.10.0
seaborn==0.13.2

# Modelling (introduced now for the same reason; first used in Week 3)
scikit-learn==1.6.1

# Notebooks and guard rails
jupyter==1.1.1
pytest==8.3.4
ruff==0.8.4

# --- Week 7: gradient boosting (optional) ---
# XGBoost is the only optional package in this file. `get_gradient_boosting()`
# uses it when it is importable and falls back to scikit-learn's
# GradientBoostingClassifier when it is not, so the week is never blocked on
# this install - see docs/curriculum/week07/learning_notes.md.
xgboost==2.1.3
