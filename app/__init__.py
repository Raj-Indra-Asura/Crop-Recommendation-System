"""The Streamlit demo application (Week 10).

One module, :mod:`app.streamlit_app`, run with ``streamlit run
app/streamlit_app.py``. It depends only on
:mod:`src.pipelines.predict_pipeline` — never on :mod:`api` — so it runs with
or without the API server.
"""
